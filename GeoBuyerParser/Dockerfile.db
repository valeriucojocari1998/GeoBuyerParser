# Use an unofficial SQLite image
FROM sqlite:latest

# Set the working directory inside the container
WORKDIR /data

# Copy your SQLite database file into the container
COPY your-database-file.sqlite /data/
